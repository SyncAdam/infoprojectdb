package com.project.views.production;

import com.project.App;
import com.project.views.MainLayout;
import com.vaadin.flow.component.Text;
import com.vaadin.flow.component.button.Button;
import com.vaadin.flow.component.charts.model.Pane;
import com.vaadin.flow.component.notification.Notification;
import com.vaadin.flow.component.orderedlayout.HorizontalLayout;
import com.vaadin.flow.router.PageTitle;
import com.vaadin.flow.router.Route;

@PageTitle("Manage your economy")
@Route(value = "Economy", layout = MainLayout.class)

public class EconomyView {
    
    // Expenses spent on every thing that has to do with the company


    // Revenue over a specific period, revenue per month week day....


    // profit/loss show how much proft there has been compared to the revenu

    
    // Specify cash flow of company meaning where is our money going, why, so that the company can optimize how to spoend money and improve on certain sections


    // Maybe if possible to show the
}

